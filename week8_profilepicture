#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int L,N,W,H;
    scanf("%d%d",&L,&N);
    while(N--)
    {
        scanf("%d%d",&W,&H);
        if(W==L&&H==L)
        {
            printf("ACCEPTED\n");
        }
        else if(W<L||H<L)
        {
            printf("UPLOAD ANOTHER\n");
        }
        else
        {
            printf("CROP IT\n");
        }
    }
    return 0;
}
